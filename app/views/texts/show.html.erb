<div class="text-center mb-4">
  <h2 class="my-3"><%= @text.title %></h2>

    <div class="mt-4">
      <%= render partial: 'text-detail', locals: { text: @text, link_nietzsche_number: false } %>
    </div>
</div>

<%= @text.text %>